[package]
name = "tls"
version = "0.1.0"
edition = "2024"

[dependencies]
axum = "0.8.9"
axum-server = { "version" = "0.8.0", "features" = [ "tls-rustls" ] }
tokio = { "version" = "1.52.1", "features" = ["full"] }
